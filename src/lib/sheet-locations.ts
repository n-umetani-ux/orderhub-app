/**
 * 稼働一覧スプレッドシート内の拠点別タブ名 → 拠点 のマッピング（単一の正本）
 *
 * 稼働一覧の読み取り（/api/sheets）と、登録時の検証（/api/sheets/validate）が
 * 同じ基準でタブを判定するために共有する。両者がズレると「検証は通るが実読みで0名」
 * という見逃しが発生するため、ここを唯一の定義箇所とする。
 */
export type Loc = "東京" | "大阪" | "福岡";

export const SHEET_LOCS: Record<string, Loc> = {
  "稼働表（東京）": "東京",
  "稼働表（大阪）": "大阪",
  "稼働表（福岡）": "福岡",
};
